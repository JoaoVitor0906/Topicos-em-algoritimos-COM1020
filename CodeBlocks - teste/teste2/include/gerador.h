#ifndef GERADOR_H
#define GERADOR_H

#include <vector>

// Funções Geradoras de Vetores
std::vector<int> gerarCrescente(int tamanho);
std::vector<int> gerarDecrescente(int tamanho);
std::vector<int> gerarAleatorio(int tamanho);

#endif // GERADOR_H
